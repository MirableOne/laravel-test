

Start:

```shell
docker-compose up -d
```

Host: 

[http://laravel-test.docker.localhost/](http://laravel-test.docker.localhost/)

Migrate:

```shell
make migrate
```

DB Seed:

```shell
make seed
```
